# --- core data stack ---
pandas
numpy

# --- SQL engine (query data with SQL; builds data/credit.duckdb) ---
duckdb

# --- modeling ---
scikit-learn
xgboost
shap

# --- plotting ---
matplotlib
seaborn

# --- notebooks ---
# NOTE: deliberately NOT installing the full `jupyter`/`jupyterlab` metapackage —
# its widget extensions have very long filenames that break on Windows' 260-char
# path limit. Cursor / VS Code only need `ipykernel` to run notebooks.
ipykernel

# --- serving (Phase 12) ---
fastapi
uvicorn
pydantic
joblib

# Added when their phase arrives, to avoid blocking the install now:
#   optbinning   -> Phase 6 (WoE / scorecard)
#   kaggle       -> only if downloading data via CLI instead of the website
